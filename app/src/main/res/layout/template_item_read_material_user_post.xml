<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingBottom="4dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="125dp">

        <ImageView
            android:id="@+id/image_view_item_thumbnail"
            android:layout_width="125dp"
            android:layout_height="match_parent"
            android:src="@drawable/chupy_box"
            android:scaleType="centerCrop"
            />

        <Button
            android:id="@+id/button_delete_post"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:background="@drawable/ic_delete_black_24dp"
            android:backgroundTint="@color/colorPrimaryDark"
            android:foreground="?attr/selectableItemBackground"
            android:layout_alignParentRight="true"
            />

        <LinearLayout
            android:layout_toRightOf="@id/image_view_item_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingRight="16dp"
            android:paddingLeft="8dp">

            <TextView
                android:id="@+id/text_view_item_category"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="20 Jul 2018"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/text_view_item_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="TEST"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/text_view_item_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="This is just a description of the item. You don't need to worry about this ..." />
            <TextView
                android:layout_marginTop="8dp"
                android:id="@+id/text_view_item_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="20 Jul 2018"
                android:textSize="12sp" />
        </LinearLayout>
    </RelativeLayout>
</LinearLayout>